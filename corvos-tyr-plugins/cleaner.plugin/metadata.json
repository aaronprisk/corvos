{
	"icon": "clean",
	"label": "Student Profile Cleaner",
	"description": "Cleans student profile on every login",
	"license": "",
	"category": "CorvOS",
	"scripts": {
		"exec": {
			"label": "Enable",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Disable",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "test -f /etc/xdg/autostart/Cleaner.desktop" }
	}
}
